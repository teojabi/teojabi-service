// Set to the HTTPS data service origin in the frontend deployment artifact.
export const DATA_API_BASE = '';
// beta-service는 자체 serve.mjs의 /api/auctions 를 쓰므로 비워 둔다.
export const AUCTION_API_BASE = '';
